---
description: Investigate browser-specific UI problems with targeted browser evidence
---

Load `browser-access`. Treat `${ARGUMENTS}` as the issue and target; ask only for missing information needed to identify the check.

For authorised browser work, use the available browser driver's skill or documented tools and select the target explicitly. Gather only the evidence the issue needs: page structure or a screenshot for visible behaviour, console or network details for failures, and a trace for performance problems.

Report the observed behaviour, supporting evidence, and next step. Make fixes only when the user requested them.
