---
description: Apply relevant Home Assistant frontend skills in current git scope
---

Load `changeset-scope`, then `branch-context-consumer` in work-scope mode. Work only within injected `<work-scope>`; `${ARGUMENTS}` may narrow it.

Load `home-assistant-frontend` and follow its routing to applicable repository-local skills. Select companions by their descriptions and the scoped code rather than loading every name matching a pattern. Repository-local conventions remain authoritative.

Make the smallest justified edits. Use `ha-frontend-testing` for scoped verification. Report the scope, skills applied, files changed, and verification result. Make no edits when no applicable improvement exists.
