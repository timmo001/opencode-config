---
description: Import or review external skills for the local skill library
---

Load `import-external-skill` and `skill-notes`. Treat `${ARGUMENTS}` as the source URL or repository; ask for it only when missing from the conversation.

Use the standalone skills checkout. For a specific skill, follow its import review workflow. For a repository, compare the available skills and let the user select additions before importing. Preserve source metadata and local adaptations.

Report the selected skills, changes, and validation. Do not commit unless explicitly requested.
